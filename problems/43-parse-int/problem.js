/*
Write a function named anotherParseInt that takes in a string and produces a number that represents what was in the string.

If there is an invalid character or the string is empty, return NaN. This functions only parse integers, not decimals. Beware an integer can be negative too.

Examples

anotherParseInt('123')    // 123
anotherParseInt('4')      // 4
anotherParseInt('-56485') // -56485
anotherParseInt('34-21')  // NaN
anotherParseInt('')       // NaN
anotherParseInt('123.45') // NaN
*/

function anotherParseInt(str) {

}


module.exports = anotherParseInt;
